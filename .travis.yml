language: python
sudo: false
cache: pip
python:
  - "2.7"
  - "3.4"
before_install:
  - pip install --upgrade pip
  - pip install wheel
  - pip install cython numpy scipy scikit-learn
  - pip install skggm
script: pytest test
