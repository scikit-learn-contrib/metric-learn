language: python
sudo: false
cache: pip
python:
  - "2.7"
  - "3.4"
before_install:
  - pip install --upgrade pip
  - pip install wheel
  - pip install codecov
  - pip install numpy scipy scikit-learn
script: 
  - pytest --cov
  - codecov
